# API server (Express). Vite dev proxy uses the same port — keep in sync with vite.config.js.
# If you see EADDRINUSE on 3001, another Node process is still running; `npm run dev` tries to free 3001 first,
# or stop the old process manually, or set API_PORT to another port (e.g. 3002) here and restart.
API_PORT=3001

# Bind address (default 0.0.0.0 — required for Docker / most PaaS so the app accepts external connections).
# HOST=0.0.0.0

# Behind nginx / Railway / Render HTTPS terminator: set so req.ip and secure cookies behave correctly.
# TRUST_PROXY=1

# Optional: restrict browser origins when frontend and API are on different hosts (comma-separated, no spaces).
# If unset, any Origin is allowed (development-friendly). In production, set to your real UI origin(s), e.g.:
# CORS_ORIGINS=https://your-app.vercel.app,https://www.yoursite.com
# Use * only for quick tests (prefer explicit origins).
# CORS_ORIGINS=

# MongoDB — required for API. Examples:
# MONGODB_URI=mongodb://127.0.0.1:27017
# MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=
MONGODB_DB=case_study_crm

# Required in production: JWT signing secret for auth tokens.
JWT_SECRET=

# Optional bootstrap admin account (created on server startup if not existing).
# This email is reserved — sign in as admin; it cannot be used for self-service "Register".
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me

# Optional: max upload size in bytes for POST /api/upload (default 12MB)
# UPLOAD_MAX_BYTES=12582912

# --- Frontend build (Vite) — set only when building the static UI, not on the API server ---
# Public URL of the API (no trailing slash). Required when the UI is hosted separately from the API
# so fetch() calls and uploaded image URLs use the correct host. Not used at runtime by Express.
# Leave unset for local dev (`npm run dev` proxies /api to the API).
# VITE_API_URL=https://api.example.com

# Optional: import path in downloaded .js for ReferenceCaseStudyForm (see server/lib/generateComponent.js)
# GENERATED_IMPORT_PATH=../../components/reference/ReferenceCaseStudyForm
